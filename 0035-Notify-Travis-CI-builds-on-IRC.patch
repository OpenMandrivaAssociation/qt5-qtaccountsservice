From e8cb945a33c7eb56e1793376417f5b9f50fcfb76 Mon Sep 17 00:00:00 2001
From: Pier Luigi Fiorini <pierluigi.fiorini@gmail.com>
Date: Wed, 9 Sep 2015 07:28:25 +0200
Subject: [PATCH 35/46] Notify Travis CI builds on IRC

---
 .travis.yml | 9 +++++++++
 1 file changed, 9 insertions(+)

diff --git a/.travis.yml b/.travis.yml
index 143159d..6c930e6 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -20,6 +20,15 @@ addons:
     build_command: "make -j 4"
     branch_pattern: coverity_scan
 
+notifications:
+  email: false
+  irc:
+    channels:
+      - "chat.freenode.net#hawaii-desktop"
+    on_success: change
+    on_failure: change
+    use_notice: true
+
 before_install:
   - if [ "$CXX" == "g++" ]; then sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test; fi
   - if [ "$CXX" == "clang++" ]; then sudo add-apt-repository -y ppa:h-rayflood/llvm; fi
-- 
1.9.5

